# 2026-08-30

- 深度性能审查仓库（未改代码）：产出 performance-review.md（本地，勿提交）。核心发现：RtGpuExecutor 每 batch 同步 waitTimeline 导致录制/GPU 串行化并传导阻塞渲染线程（H1）；云透过率/卷云 per-ray 解析求值未 dome-LUT 化（H2/H3）；any-hit 固定 mip0、影子载荷丢 rayCone（H4）；FG acquire 5s 超时阻塞渲染线程（H5）；TLAS 每帧全量 build 未用 refit（M1）。总体代码性能成熟度高于典型 mod 水准。
- 用户补充约束（已写入报告"优化约束"节）：实例 4K 渲染，任何优化不得牺牲 4K 清晰度/降渲染分辨率；已启用 DLSS 帧生成，优化须与 DLSS 协同而非降分辨率换帧率。此为该项目的长期性能约束，后续所有优化建议须遵守。
- 第一阶段优化已实施并验证（compileJava + test 通过；14 个着色器阶段 slangc+spirv-val 通过含 SER 变体）：H1 RtGpuExecutor in-flight 队列化（MAX_IN_FLIGHT=8 背压、1ms 轮询 reap、STOP 时 drain）；H4 影子载荷携带 rayCone（LOD 辅助函数放 world_core 而非 world_common——后者被 sky_lut 计算阶段导入，RayTCurrent 在计算阶段非法；visibility/shadeReservoir 加默认参数 0u 向后兼容）；H3 sky.rmiss skyDome/sunExtinction 缓存；H5 acquire 超时 5s→50ms；M1 TLAS FAST_TRACE→FAST_BUILD。后续项：swapchain minImageCount 扩容钩子、TLAS refit、H2/M2 云 dome 化、L 项。gradlew test 需 -x bundleNgxNatives（本机缺预编译 shim，与改动无关）。
- 第二阶段 H2 已实施并验证（15 阶段 slangc+spirv-val、compileJava、test 全通过）：新 pass shaders/pipelines/sky_lut/cloud_shadow.comp.slang 按帧烘焙世界 XZ 锚定云影图（1024²，4 方块/纹素，4096 窗口，camOffset 居中），lighting.slang celestialCloudShadow 云底以下一次双线性采样、云内/云上回退解析；关键设计：中点高度与截断层内长度与原点高度无关（flat-slab 近似，曲率误差 ~1e-2 方块），烘焙与 NEE 共用 dominantCelestialLight。改动涉及 sky_lut/bindings.slang(+2)、world/bindings.slang(+binding15)、GenerateRtBindings.groovy 名称表（严格校验 missing/unexpected）、RtSkyLut（图/管线/描述符/dispatch/destroy）、RtPipeline（RAYGEN 阶段布局项+setCloudShadow）、RtComposite（绑定点）。H3 卷云入烘经评估放弃（违反 4K 清晰度约束）。
- 第三轮（L 批逐项核查）：仅 L4 实施（RtTerrain dispatch 后单次 O(n) 移除替代 per-key indexOf，利用"幸存者必在 queuedReextract"不变式）。逐项驳回：L6 FRAPI 已按图集缓存 SpriteFinder（TextureAtlasMixin volatile+upload 失效）；L10 MemoryStack 凸包分配纳秒级无 GC 压力；L7 Frustum 平面提取仅在构造器无重算 API；L1/L3 字节级垃圾不值得动 ABI；L2 缓存名牌遮挡会引入可见闪变；L8 breadcrumb 是崩溃取证不能采样；L5 无规则包时恒零成本；L9 opt-in 诊断。M3 暂缓：焦散逐样本光源抖动在 DLSS-RR 下物理柔化是有意设计，烘图会固化图样（已验证 det 为 h 二次式、双通道可精确还原，方案备妥）。M4/M5/M2/TLAS refit/swapchain minImageCount 均需游戏内验证。
